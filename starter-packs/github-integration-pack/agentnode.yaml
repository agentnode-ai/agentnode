manifest_version: "0.1"
package_id: "github-integration-pack"
package_type: "toolpack"
name: "Github Integration Pack"
publisher: "agentnode"
version: "1.0.0"
summary: "Interact with GitHub repositories, issues, and pull requests."
description: "Interact with GitHub repositories, issues, and pull requests."
runtime: "python"
install_mode: "package"
hosting_type: "agentnode_hosted"
entrypoint: "github_integration_pack.tool"

capabilities:
  tools:
    - name: "code_analysis"
      capability_id: "code_analysis"
      description: "Interact with GitHub repositories, issues, and pull requests."
      input_schema:
        type: "object"
        properties:
          token: {type: "string"}
          operation: {type: "string"}
          repo: {type: "string", default: ""}
        required: ['token', 'operation']
  resources: []
  prompts: []

tags: ["github", "integration"]
categories: ["development"]

compatibility:
  frameworks: ["generic"]
  python: ">=3.10"
  dependencies:
    - "PyGithub>=2.1"

permissions:
  network: {level: "unrestricted"}
  filesystem: {level: "none"}
  code_execution: {level: "none"}
  data_access: {level: "input_only"}
  user_approval: {required: "never"}

upgrade_roles: ["code_analysis"]
recommended_for:
  - agent_type: "general-assistant"
    missing_capability: "code_analysis"

install_strategy: "local"
fallback_behavior: "skip"
policy_requirements:
  min_trust_level: "unverified"
  requires_approval: false

security:
  signature: null
  provenance:
    source_repo: null
    commit: null
    build_system: null

support:
  homepage: "https://agentnode.net/packages/github-integration-pack"
  issues: "https://github.com/agentnode-ai/agentnode/issues"

deprecation_policy: "6-months-notice"
