[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "security-scanner-agent"
version = "1.0.0"
description = "Run a comprehensive security scan on a codebase: SAST, dependency vulnerabilities, secret detection, and compliance chec"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
