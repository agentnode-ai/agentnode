[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "report-generator-agent"
version = "2.0.0"
description = "Transform raw data (CSV, JSON) into a formatted report with statistics and summary."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
