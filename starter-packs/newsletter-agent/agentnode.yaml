manifest_version: "0.2"
package_id: "newsletter-agent"
package_type: "agent"
name: "Newsletter Agent"
publisher: "agentnode"
version: "2.0.0"
summary: "Curate top stories on a topic, summarize them, and draft a newsletter email."
description: |
  Create a newsletter by aggregating stories, summarizing articles, and drafting an email.

  Uses AgentContext contract v1: calls tools via context.run_tool()
  with real data flow between steps and structured output synthesis.

runtime: "python"
install_mode: "package"
hosting_type: "agentnode_hosted"
entrypoint: "newsletter_agent.agent"

capabilities:
  tools:
    - name: "newsletter"
      capability_id: "newsletter"
      description: "Curate top stories on a topic, summarize them, and draft a newsletter email."
      entrypoint: "newsletter_agent.agent:run"
      input_schema:
        type: "object"
        properties:
          goal:
            type: "string"
            description: "The objective for the agent"
        required: ["goal"]
      output_schema:
        type: "object"
        properties:
          result:
            type: "object"
          done:
            type: "boolean"
  resources: []
  prompts: []

agent:
  entrypoint: "newsletter_agent.agent:run"
  goal: "Create a newsletter by aggregating stories, summarizing articles, and drafting an email."
  tool_access:
    allowed_packages: []
  limits:
    max_iterations: 10
    max_tool_calls: 50
    max_runtime_seconds: 300
  termination:
    stop_on_final_answer: true
    stop_on_consecutive_errors: 3
  isolation: "thread"
  state:
    persistence: "none"

compatibility:
  frameworks: ["generic"]
  python: ">=3.10"
  dependencies: []

permissions:
  network:
    level: "unrestricted"
  filesystem:
    level: "none"
  code_execution:
    level: "none"
  data_access:
    level: "input_only"
  user_approval:
    required: "never"

tags: ["agent", "newsletter", "email", "content", "curation"]
categories: ["content"]
