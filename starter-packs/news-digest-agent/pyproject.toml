[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "news-digest-agent"
version = "1.0.0"
description = "Aggregate news from multiple sources on a topic, summarize key stories, and optionally translate for multilingual digest"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
