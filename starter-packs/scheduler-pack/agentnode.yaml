manifest_version: "0.1"
package_id: "scheduler-pack"
package_type: "toolpack"
name: "Scheduler Pack"
publisher: "agentnode"
version: "1.0.0"
summary: "Cron parsing, event scheduling, and recurrence management using stdlib."
description: "Cron parsing, event scheduling, and recurrence management using stdlib."
runtime: "python"
install_mode: "package"
hosting_type: "agentnode_hosted"
entrypoint: "scheduler_pack.tool"

capabilities:
  tools:
    - name: "scheduling"
      capability_id: "scheduling"
      description: "Schedule operations: parse cron, manage events, compute occurrences."
      input_schema:
        type: "object"
        properties:
          operation: {type: "string"}
        required: ['operation']
  resources: []
  prompts: []

tags: ["scheduler"]
categories: ["productivity"]

compatibility:
  frameworks: ["generic"]
  python: ">=3.10"

permissions:
  network: {level: "none"}
  filesystem: {level: "none"}
  code_execution: {level: "none"}
  data_access: {level: "input_only"}
  user_approval: {required: "never"}

upgrade_roles: ["scheduling"]
recommended_for:
  - agent_type: "general-assistant"
    missing_capability: "scheduling"

install_strategy: "local"
fallback_behavior: "skip"
policy_requirements:
  min_trust_level: "unverified"
  requires_approval: false

security:
  signature: null
  provenance:
    source_repo: null
    commit: null
    build_system: null

support:
  homepage: "https://agentnode.net/packages/scheduler-pack"
  issues: "https://github.com/agentnode-ai/agentnode/issues"

deprecation_policy: "6-months-notice"
