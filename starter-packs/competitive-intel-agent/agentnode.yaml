manifest_version: "0.2"
package_id: "competitive-intel-agent"
package_type: "agent"
name: "Competitive Intelligence Agent"
publisher: "agentnode"
version: "2.1.0"
summary: "Analyze competitors by scraping web presence, monitoring news, and producing a competitive report."
description: |
  Gather competitive intelligence by searching the web, extracting content, and summarizing into an analysis.

  Uses AgentContext contract v1: calls tools via context.run_tool()
  with real data flow between steps and structured output synthesis.

runtime: "python"
install_mode: "package"
hosting_type: "agentnode_hosted"
entrypoint: "competitive_intel_agent.agent"

capabilities:
  tools:
    - name: "competitive_intel"
      capability_id: "competitive_intel"
      description: "Analyze competitors by scraping web presence, monitoring news, and producing a competitive report."
      entrypoint: "competitive_intel_agent.agent:run"
      input_schema:
        type: "object"
        properties:
          goal:
            type: "string"
            description: "The objective for the agent"
        required: ["goal"]
      output_schema:
        type: "object"
        properties:
          result:
            type: "object"
          done:
            type: "boolean"
  resources: []
  prompts: []

agent:
  entrypoint: "competitive_intel_agent.agent:run"
  goal: "Gather competitive intelligence by searching the web, extracting content, and summarizing into an analysis."
  system_prompt: |
    You are a competitive intelligence analyst.
    Research competitors by analyzing their web presence, recent news,
    product offerings, and market positioning. Produce a structured
    competitive analysis report with actionable insights.
  tool_access:
    allowed_packages:
      - "document-summarizer-pack"
      - "web-search-pack"
      - "webpage-extractor-pack"
  limits:
    max_iterations: 10
    max_tool_calls: 50
    max_runtime_seconds: 300
  termination:
    stop_on_final_answer: true
    stop_on_consecutive_errors: 3
  isolation: "thread"
  state:
    persistence: "none"

compatibility:
  frameworks: ["generic"]
  python: ">=3.10"
  dependencies: []

permissions:
  network:
    level: "unrestricted"
  filesystem:
    level: "none"
  code_execution:
    level: "none"
  data_access:
    level: "input_only"
  user_approval:
    required: "never"

tags: ["agent", "research", "competitive", "business"]
categories: ["research"]
