[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "newsletter-agent"
version = "2.0.0"
description = "Curate top stories on a topic, summarize them, and draft a newsletter email."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
